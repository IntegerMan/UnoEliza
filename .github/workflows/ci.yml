name: CI

on:
  push:
    branches:
      - main
      - release/**

  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - release/**
env:
  STEP_TIMEOUT_MINUTES: 60

jobs:
  smoke_test:
    name: Smoke Test (Debug Build of MattEland.AI.Assistant.UnoClient)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Install Dependencies
        timeout-minutes: ${{ fromJSON(env.STEP_TIMEOUT_MINUTES) }}
        uses: "./.github/steps/install_dependencies"

      # Add  MSBuild to the PATH: https://github.com/microsoft/setup-msbuild
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.4.1

      - name: Build MattEland.AI.Assistant.UnoClient (Debug)
        shell: pwsh
        run: msbuild ./MattEland.AI.Assistant.UnoClient/MattEland.AI.Assistant.UnoClient.csproj /r

  unit_test:
    name: Unit Tests
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Install Dependencies
        timeout-minutes: ${{ fromJSON(env.STEP_TIMEOUT_MINUTES) }}
        uses: "./.github/steps/install_dependencies"

      # Add  MSBuild to the PATH: https://github.com/microsoft/setup-msbuild
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.4.1

      - name: Build MattEland.AI.Assistant.UnoClient.Tests (Release)
        shell: pwsh
        run: msbuild ./MattEland.AI.Assistant.UnoClient.Tests/MattEland.AI.Assistant.UnoClient.Tests.csproj /p:Configuration=Release /p:OverrideTargetFramework=net9.0 /r

      - name: Run Unit Tests
        shell: pwsh
        run: dotnet test ./MattEland.AI.Assistant.UnoClient.Tests/MattEland.AI.Assistant.UnoClient.Tests.csproj --no-build -c Release --logger GitHubActions --blame-crash --collect:"XPlat Code Coverage" -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=opencover
